// This component is no longer used — the page.tsx now renders initiative data inline.
// Kept as a stub to avoid import errors from other files.
export function InitiativesList() {
  return null;
}
